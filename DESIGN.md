# 何以为本网站设计系统

本文记录网站当前已经实现并通过截图验收的视觉与交互规则。它不是概念提案；设计依据为 `PRODUCT.md`、现有组件与样式、`references/locked/v1/` 以及 `qa/` 中的真实页面截图。

## 设计理念

“何以为本”是一份围绕技术、商业与生活展开的个人独立网络刊物。视觉先让人记住“问题之下”的山水意象，再安静地进入文章。

1. 文章是主体；山水、纸纹和动效只建立气质。
2. 像刊物，不像内容门户；首页只出现一次文章区。
3. 当代山水，不做古风仿制；山水负责构图、留白和转场。
4. 静态内容优先；公开页面生成真实 HTML。
5. 节制胜过装饰；秩序来自字号、留白、细线和图像裁切。
6. 固定品牌语句为“何以为本”“问题之下”“在技术、商业与生活之间。”

## 颜色

颜色 token 定义在 `app/styles/global.css`，新增页面应复用语义 token。

| Token | 数值 | 用途 |
| --- | --- | --- |
| `--paper` | `#f3f0e8` | 全站纸面背景 |
| `--paper-deep` | `#e8e5da` | 纸面次级层次 |
| `--ink` | `#173d3a` | 主文字、标题、导航 |
| `--ink-soft` | `#46615d` | 摘要、日期、说明 |
| `--green` | `#6d9b88` | 年份与弱强调 |
| `--blue` | `#7186a6` | 山水冷色备用色 |
| `--coral` | `#de765f` | 品牌方点、活动线、小面积信号 |
| `--focus` | `#8f3b2e` | 键盘焦点轮廓 |
| `--line` | `18% --ink` | 分隔线 |

珊瑚色不用于大段文字或大面积背景；界面文字不得依赖混合模式维持可读性。当前仅支持浅色模式。

## 字体与排版

- `--sans`：本地 `Noto Sans SC Variable`，用于导航、界面、页面与文章标题。
- `--serif`：本地 `Noto Serif SC Variable`，用于正文、摘要与叙述。
- 禁止依赖外部字体服务；全站 `font-synthesis: none`。
- 首页主标题：`clamp(4.4rem, 8.4vw, 8rem)`；移动端独立缩放。
- 文章主标题：`clamp(3rem, 5.6vw, 5.2rem)`。
- 正文：`clamp(1.08rem, 1.25vw, 1.22rem)`，行高 `2.05`；移动端 `1.04rem / 2`。
- 正文最大宽度 `44rem`，超宽屏也不拉长行宽。
- 大标题紧字距，小标签与导航可使用轻微正字距。

## 尺度与留白

- 页面最小宽度 `320px`。
- 全局横向页边距 `clamp(1.5rem, 5vw, 6.5rem)`；`760px` 以下为 `1.35rem`。
- 页头高度 `clamp(5.25rem, 9vw, 7.5rem)`。
- 内容段主要使用 `3rem–6rem` 的纵向间距，不靠背景块分区。
- 分隔线统一为 `1px solid var(--line)`。
- 交互控件最小点击高度 `44px`。
- 一般不使用圆角；不要建立卡片化体系。

## 全局骨架

页面由统一页头、主内容和页尾构成。

- 页头左侧为“何以为本”与珊瑚方点，右侧仅保留“索引”。
- 索引抽屉只有“文章”“关于”“联系”三个入口；桌面从右侧进入，手机占满宽度。
- 抽屉打开时锁定滚动，背景与字标设为 `inert`，焦点限制在抽屉与触发按钮内。
- 页尾只显示“何以为本 Lab”、邮箱和版权。
- 分类、标签和系列只能作为轻量索引，不进入主导航。

## 页面结构

### 首页

首页只包含山水 Hero 与三篇文章。Hero 文字处于左侧安全区，山体重心位于右下。Hero 与文章区以重叠的纸色雾幕连续衔接，不使用硬切边或独立滚动提示。第一篇文章作为视觉主条目，另两篇收敛；区尾只有“查看全部文章”。禁止第二套推荐区、卡片墙或分类入口。

### 文章归档

顺序为页面标题、轻量筛选、按年份分组的文章。桌面端年份独占左列；移动端重新排列日期、分类和标题，不缩小桌面网格。归档山雾始终使用全幅画布并在底部退入纸色，不允许用固定宽度裁出可见的矩形边界。

### 长文页面

文章页由山水雾景题头、导语、`44rem` 正文和结束导航组成。正文标题使用无衬线体，正文使用衬线体；引用、链接和分隔只使用细珊瑚线强调。

### 关于页

关于页保留山谷包围文字的开放构图。桌面五列写作方向在移动端改为单列分组；不得包装为履历或通用卡片。

### 错误与空状态

404 使用“这页不在山中。”和返回首页入口。错误页不展示技术堆栈，不引入独立插画风格。

## 山水素材

- 锁定参考图：`references/locked/v1/01-hero.png` 至 `05-about.png`。
- PNG 母版：`static/images/landscape/`。
- 线上 WebP：由 `scripts/prepare-assets.mjs` 生成到 `static/images/landscape/web/`。
- 只修改 PNG 母版，不直接手工修改生成 WebP。
- Hero 分为天空、远山、中景、近景和静态降级图；其余页面各有独立雾景。
- 山水是装饰信息，使用空 `alt` 或 `aria-hidden`，不得进入正文流充当无关配图。
- 纸纹固定覆盖，透明度 `.035`，不得拦截指针。
- 不得为了掩盖实现偏差把锁定参考图直接铺成页面背景。

## 动效

首页滚动进度限制在 `0–1`，由 `requestAnimationFrame` 更新。三层山体使用不同方向与幅度的 CSS transform 形成轻景深，两团雾气以 18 秒和 23 秒缓慢漂移。

索引抽屉使用 `.65s` 位移，遮罩使用 `.5s` 淡入。文章正文、归档和关于页不加入滚动入场动画。禁止自动轮播、逐字动画、光标尾迹、粒子、视频背景和持续闪烁。

在 `prefers-reduced-motion: reduce` 下：关闭平滑滚动和持续动效，隐藏分层山水与雾气，显示 `hero-static.webp`，且不注册 Hero 滚动监听。

## 响应式

主要断点为 `760px`；其余尺寸优先使用 `clamp()`、最大宽度和弹性网格。

- 手机 Hero 山水允许扩大到 150% 并重新裁切，必须保留主峰与标题安全区。
- 首页文章、归档和页尾转为单列。
- 筛选可横向滚动，不强行压成多行。
- 正文宽度为 `calc(100% - 2.7rem)`。
- 超宽屏保持内容最大宽度，不让正文与标题漂散。
- 变更至少对照 `qa/desktop/`、`qa/ultrawide/`、`qa/mobile/`。

## 无障碍

- 文档语言为 `zh-CN`，保留真实语义元素与标题层级。
- 键盘焦点使用 `2px solid var(--focus)`、外偏移 `5px`，不得删除。
- 当前筛选同时使用颜色、字重或下划线，颜色不能是唯一信号。
- 索引触发器必须保留 `aria-expanded`、`aria-controls`、Escape 关闭、焦点循环与关闭回焦。
- 装饰山水不进入读屏顺序。
- 公开正文必须存在于构建生成的 HTML，不得改成仅客户端加载。

## 禁用模式

不得引入：个人简历首页、资讯门户、卡片瀑布流、玻璃拟态、发光球、工程线、节点图、纯黑赛博背景、随机渐变、古风书法字、印章堆叠、大面积珊瑚色、装饰统计、无限正文行宽、仅悬停可见的信息，以及不尊重减少动态效果的动画。

## 维护约束

1. `PRODUCT.md` 管产品定位与固定文案；`app/styles/global.css` 是视觉 token 真源。
2. 未经确认不得替换 `references/locked/v1/`；方向改变应建立新版本。
3. 有意视觉变更需更新三档 QA 截图并记录原因。
4. 新山水母版必须加入 `prepare-assets.mjs` 的明确清单。
5. 首页继续只显示三篇公开文章，不建立第二套查询或入口。
6. 所有公开 URL、别名、RSS、站点地图、404、图标与分享图保持稳定。
7. 交互变更须以键盘完成打开、遍历、关闭和返回焦点。
8. 视觉调整后必须运行内容测试、类型检查、生产构建、链接检查与 identity 证据验证。
9. 未经明确授权，不发布文章、不部署、不推送 Git、不更改定时任务。
